# Chapter 5 evidence snapshot — 2026-07-03 ICT

This file records fresh evidence used in `Doc/CareerFit-Thesis-Report.docx`. It does not declare the system production-ready.

## Environment

- Git commit observed before evaluation: `e92e3d847992d7b628bcfbbcef9a57ab32677547`.
- Worktree was dirty before evaluation; results describe the working tree, not the commit alone.
- Windows host, Java `21.0.1`, Node.js `20.16.0`, npm `10.8.1`.
- Docker Desktop server `29.5.3`, Linux containers, Docker Compose `v5.1.4`.
- Testcontainers `1.21.4`, PostgreSQL `16.14` container.
- Backend: Spring Boot `3.2.5`; frontend: Vite `5.4.21`.

## Backend suite

Command:

```powershell
cd Backend/careerfit-backend
.\mvnw.cmd test
```

Result:

- Exit code: `0`.
- Surefire XML: 15 suites, 63 tests, 0 failures, 0 errors, 0 skipped.
- Aggregated test time from XML: 81.564 seconds; wall-clock command time: 111.9 seconds.
- Flyway validated and applied 14 migrations in Testcontainers databases.
- Warning: Flyway 9.22.3 reports PostgreSQL 16.14 newer than its tested support level (latest reported as PostgreSQL 15).

## Controlled Rocchio benchmark

Artifact: `evaluation/result.json` generated by `AlgorithmEvaluatorTest`.

- Dataset: 50 jobs, 100 unique CVs, 300 training pairs, 300 holdout pairs.
- Dataset SHA-256: `6e935639ba6d3290dca8ad91a35d714c5e30c7e69a59af23ddbcf89fcc5cc2f2`.
- Parameters: alpha `1.0`, beta `0.75`, gamma `0.15`.

| Metric | Baseline | Rocchio | Delta |
|---|---:|---:|---:|
| Precision@5 | 0.012000 | 0.168000 | +0.156000 |
| Recall@5 | 0.060000 | 0.840000 | +0.780000 |
| nDCG@3 | 0.030000 | 0.810000 | +0.780000 |
| nDCG@5 | 0.037737 | 0.817737 | +0.780000 |
| nDCG@10 | 0.050424 | 0.830424 | +0.780000 |
| MRR | 0.058755 | 0.823617 | +0.764862 |
| HitRate@5 | 0.060000 | 0.840000 | +0.780000 |
| Coverage | 1.000000 | 1.000000 | 0 |

Three additional benchmark executions returned exit code 0 and exactly the same dataset hash, baseline nDCG@5, Rocchio nDCG@5, and delta. Wall times were 62.67, 66.80, and 68.66 seconds.

Important limitation: all three isolated benchmark logs contain a background `StaleObjectStateException` involving a Matching row. JUnit still returns success because the exception occurs in background scheduling/concurrency outside the benchmark assertion. Therefore the metric is deterministic, but the run is not operationally clean.

Logs: `evidence/chapter5-benchmark-run-1.log` through `chapter5-benchmark-run-3.log`.

## Frontend build

Command:

```powershell
cd Frontend
npm run build
```

Result:

- Exit code: `0`.
- TypeScript check and Vite build succeeded; 2,475 modules transformed.
- Output: HTML 1.07 kB, CSS 61.91 kB (12.27 kB gzip), JavaScript 802.15 kB (238.38 kB gzip).
- Vite warning: JavaScript chunk exceeds the 500 kB warning threshold; code splitting/manual chunks should be considered.

## Chromium P0 E2E

Runtime: Compose PostgreSQL on host port 5433, backend on host port 8080, Vite on 127.0.0.1:5173.

Command:

```powershell
$env:BASE_URL='http://127.0.0.1:5173'
npx playwright test --project=chromium --reporter=line
```

Result: 4 passed in 33.9 seconds.

1. Guest search and job detail.
2. Candidate login, apply, and withdraw.
3. Recruiter creates a JD and verifies it in the UI.
4. Administrator suspends and reactivates a user.

Limitations:

- Only Chromium was executed; Firefox and WebKit were not included in this evidence snapshot.
- The suite uses seeded accounts and the current local database.
- The recruiter test creates a timestamped Job and does not automatically delete it.
- This is scripted acceptance/E2E evidence, not a user study.

## API security checks

| Check | Observed status |
|---|---:|
| Public job search without token | 200 |
| Candidate CV endpoint without token | 401 |
| Auth endpoint with invalid Basic scheme | 401 |
| Current account with valid Candidate bearer token | 200 |
| Admin dashboard with Candidate bearer token | 403 |

## Runtime health and monitoring

- `/actuator/health`: `503 DOWN` with details hidden.
- `/actuator/health/liveness`: `200 UP`.
- `/actuator/health/readiness`: `200 UP`.
- `/actuator/prometheus`: `200`, 349,580 bytes in the observed request.
- Public jobs endpoint: `200` and reported 991 jobs in the local database.

The aggregate health failure was not root-caused during this pass. Liveness/readiness and core API success do not justify reporting aggregate monitoring health as green.

## Small local latency sample

Thirty sequential warm requests to `GET /api/jobs/search?page=0&size=20` on localhost:

- Mean: 61.79 ms.
- Median (p50): 55.20 ms.
- p95: 85.11 ms.
- Minimum: 44.99 ms.
- Maximum: 99.32 ms.

This is a single-client, sequential local sample without controlled load, network latency, warm-up protocol, or repeated experimental batches. It must not be interpreted as production capacity or throughput.
